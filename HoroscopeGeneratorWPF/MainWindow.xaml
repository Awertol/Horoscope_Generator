<Window x:Class="HoroscopeGeneratorWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HoroscopeGeneratorWPF"
        mc:Ignorable="d"
        Title="Astral Guide" Height="900" Width="600" MinHeight="800" MinWidth="500"
        WindowStartupLocation="CenterScreen" Background="#0F0C29">
    <Window.Resources>
    <LinearGradientBrush x:Key="MainBackground" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#0F2027" Offset="0"/>
        <GradientStop Color="#203A43" Offset="0.5"/>
        <GradientStop Color="#2C5364" Offset="1"/>
    </LinearGradientBrush>

    <Style x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="Border" 
                            Background="#33FFFFFF" 
                            CornerRadius="20" 
                            BorderThickness="1" 
                            BorderBrush="#44FFFFFF">
                        <Path x:Name="Arrow"
                              Data="M 0 0 L 4 4 L 8 0"
                              Stroke="White"
                              StrokeThickness="2"
                              HorizontalAlignment="Right"
                              VerticalAlignment="Center"
                              Margin="0,0,15,0"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#44FFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Arrow" Property="RenderTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="180" CenterX="4" CenterY="2"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Height" Value="45"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="15,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton Style="{StaticResource ComboBoxToggleButton}" 
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press"/>
                        
                        <ContentPresenter x:Name="ContentSite"
                                          IsHitTestVisible="False" 
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"/>

                        <Popup x:Name="PART_Popup" 
                               Placement="Bottom" 
                               IsOpen="{TemplateBinding IsDropDownOpen}" 
                               AllowsTransparency="True" 
                               Focusable="False" 
                               PopupAnimation="Slide">
                            <Border x:Name="DropDownBorder" 
                                    Background="#1A1A2E" 
                                    CornerRadius="15" 
                                    BorderThickness="1" 
                                    BorderBrush="#44FFFFFF" 
                                    MinWidth="{TemplateBinding ActualWidth}" 
                                    MaxHeight="300" 
                                    Margin="0,5,0,0">
                                <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="ItemBorder" 
                            Background="{TemplateBinding Background}" 
                            CornerRadius="10" 
                            Margin="2"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="#33E94560"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="#E94560"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ModernButton" TargetType="Button">
        <Setter Property="Background" Value="#E94560"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Height" Value="50"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="25">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FF2E63"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</Window.Resources>

    <Grid Margin="30,30,30,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="1.5*"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="2*"/>   </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Astral Guide" 
                   Foreground="White" FontSize="32" FontWeight="Thin" 
                   HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock.Effect>
                <DropShadowEffect BlurRadius="10" Color="Cyan" ShadowDepth="0"/>
            </TextBlock.Effect>
        </TextBlock>

        <Border Grid.Row="1" CornerRadius="20" ClipToBounds="True" Margin="0,10" BorderBrush="#55FFFFFF" BorderThickness="1">
            <Image Source="horoskop.png" Stretch="Uniform" />
        </Border>

        <StackPanel Grid.Row="2" Margin="0,10">
            <TextBlock Text="Vyberte své znamení:" Foreground="#AAA" Margin="5,0,0,5"/>
            <ComboBox Name="ZnameniSelection" >
                <ComboBoxItem Content="Beran"/>
                <ComboBoxItem Content="Býk"/>
                <ComboBoxItem Content="Blíženci"/>
                <ComboBoxItem Content="Rak"/>
                <ComboBoxItem Content="Lev"/>
                <ComboBoxItem Content="Panna"/>
                <ComboBoxItem Content="Váhy"/>
                <ComboBoxItem Content="Štír"/>
                <ComboBoxItem Content="Střelec"/>
                <ComboBoxItem Content="Kozoroh"/>
                <ComboBoxItem Content="Vodnář"/>
                <ComboBoxItem Content="Ryby"/>
            </ComboBox>
        </StackPanel>

        <Button Grid.Row="3" Content="VYGENEROVAT OSUD" Style="{StaticResource ModernButton}" Margin="0,10"
                x:Name="btnGenerate"
                Click="BtnGenerate_OnClick"/>

        <Border Grid.Row="4" Background="#22000000" CornerRadius="15" Margin="0,10,0,0" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="HoroskopOutput" 
                           Text="Zde se objeví Tvá věštba..." 
                           Foreground="White" 
                           FontSize="18" 
                           TextWrapping="Wrap" 
                           FontStyle="Italic" 
                           LineHeight="25"
                           TextAlignment="Center"
                           VerticalAlignment="Center"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
